Investigated so far:

prepare a label in Microsoft Purview that creates a record (forever)
publish this label
wait about 12 hours

To lock a document apply the label (LockDocument.ps1)

To unlock a document remove the label (RemoveLabelOnDocument.ps1)
